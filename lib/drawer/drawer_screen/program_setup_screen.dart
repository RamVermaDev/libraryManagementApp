import 'package:flutter/material.dart';

class ProgramSetupScreen extends StatelessWidget {
  const ProgramSetupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Program Setup'));
  }
}
